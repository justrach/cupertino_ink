//
//  Color+Extensions.swift
//  app
//
//  Created by Rach Pradhan on 4/18/25.
//

import SwiftUI

// Define custom colors globally
extension Color {
    static let nuevoOrange = Color(red: 1.0, green: 0.3, blue: 0.1) // Adjusted orange
    static let nuevoDarkGray = Color(white: 0.15)
    static let nuevoLightGray = Color(white: 0.85)
    static let nuevoInputBackground = Color(white: 0.1) // Slightly darker for input
    static let nuevoStroke = Color(white: 0.3) // Subtle stroke
} 